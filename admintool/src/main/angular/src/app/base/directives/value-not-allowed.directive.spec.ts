import { ValueNotAllowedDirective } from './value-not-allowed.directive';

describe('ValueNotAllowedDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueNotAllowedDirective();
    expect(directive).toBeTruthy();
  });
});
