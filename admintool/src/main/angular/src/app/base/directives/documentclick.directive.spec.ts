import { DocumentclickDirective } from './documentclick.directive';

describe('DocumentclickDirective', () => {
  it('should create an instance', () => {
    const directive = new DocumentclickDirective();
    expect(directive).toBeTruthy();
  });
});
