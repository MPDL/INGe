es_cluster_name = elastic_inge
es_transport_ips = 134.76.28.200:8080
es_transport_backup = 10.20.2.11:7777 10.20.2.95:8888

item_index_name = pure
item_index_type = item
user_index_name = db_users
user_index_type = account
usergroup_index_name = user_accounts
usergroup_index_type = group
context_index_name = pure_contexts
context_index_type = context
organization_index_name = organizational_units
organization_index_type = organization

# Authentication URLs
auth.token.url = ${auth.token.url}
auth.users.url = ${auth.users.url}

inge.database.driver.class=${inge.database.driver.class}
inge.database.user.name=${inge.database.user.name}
inge.database.user.password=${inge.database.user.password}
inge.database.jdbc.url=${inge.database.jdbc.url}




# Internal base HTTP URL and optionally port of the eSciDoc framework instance which is used for this PubMan instance. This URL can only be reachable from the server on which PubMan is running.
# Thus, if eSciDoc runs on the same server as PubMan, a local (localhost) address can be used
# e.g http://my-escidoc-instance.com or http://localhost:8080
escidoc.framework_access.framework.url=https://qa-coreservice.mpdl.mpg.de

# External base HTTP URL and optionally port of the eSciDoc framework instance which is used for this PubMan instance. This URL must be reachable from the client's browser.
# In a productive environment, no local (localhost) address can be used here.
# e.g http://my-escidoc-instance.com
escidoc.framework_access.login.url=https://qa-coreservice.mpdl.mpg.de

# Identifier of the content type "publication" in the actual framework instance
escidoc.framework_access.content-model.id.publication=escidoc:persistent4

# System administrator username and password of eSciDoc framework instance
framework.admin.username=roland
framework.admin.password=BenuLKepNc
