
# URL of the FIZ framework instance
escidoc.framework_access.framework.url=${escidoc.framework_access.framework.url}

# URL of the FIZ framework login
escidoc.framework_access.login.url=${escidoc.framework_access.login.url}

# Identifier of the content type "publication" in the actual framework instance
escidoc.framework_access.content-model.id.publication=${escidoc.framework_access.content-model.id.publication}

# Identifier of the test context in the actual framework instance
escidoc.framework_access.context.id.test = ${escidoc.framework_access.context.id.test}

# E-Mail settings
escidoc.pubman_presentation.email.mailservername=${escidoc.pubman_presentation.email.mailservername}
escidoc.pubman_presentation.email.sender=${escidoc.pubman_presentation.email.sender}
escidoc.pubman_presentation.email.withauthentication=${escidoc.pubman_presentation.email.withauthentication}
escidoc.pubman_presentation.email.authenticationuser=${escidoc.pubman_presentation.email.authenticationuser}
escidoc.pubman_presentation.email.authenticationpwd=${escidoc.pubman_presentation.email.authenticationpwd}

# eSciDoc-ObjId of the external Organisation
escidoc.pubman.external.organisation.id=${escidoc.pubman.external.organisation.id}
framework.admin.username=${framework.admin.username}
framework.admin.password=${framework.admin.password}
framework.admin.id=${framework.admin.id}
framework.admin.displayname=${framework.admin.displayname}

framework.librarian.username=${framework.librarian.username}
framework.librarian.password=${framework.librarian.password}
framework.librarian.id=${framework.librarian.id}
framework.librarian.displayname=${framework.librarian.displayname}

framework.author.username=${framework.author.username}
framework.author.password=${framework.author.password}
framework.author.id=${framework.author.id}
framework.author.displayname=${framework.author.displayname}

framework.editor.username=${framework.editor.username}
framework.editor.password=${framework.editor.password}
framework.editor.id=${framework.editor.id}
framework.editor.displayname=${framework.editor.displayname}

framework.scientist.username=${framework.scientist.username}
framework.scientist.password=${framework.scientist.password}
framework.scientist.id=${framework.scientist.id}
framework.scientist.displayname=${framework.scientist.displayname}
